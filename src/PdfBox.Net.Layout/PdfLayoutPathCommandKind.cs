namespace PdfBox.Net.Layout;

/// <summary>
/// A supported vector path command in normalized page coordinates.
/// </summary>
public enum PdfLayoutPathCommandKind
{
    MoveTo,
    LineTo,
    CurveTo,
    ClosePath
}
